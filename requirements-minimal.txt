# Minimal dependencies for drawing with known templates:
#     r2dt.py draw --force_template <model_id>
#
# All pure Python, so they install without a build toolchain on any
# supported Python version. See docs/installation.md.

click==7.0
colorhash==1.2.1
requests==2.31.0
rich==13.5.2
tqdm==4.66.5
