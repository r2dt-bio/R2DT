# Full R2DT runtime dependencies.
#
# For the drawing-only subset (r2dt.py draw --force_template) see
# requirements-minimal.txt. Development/test and documentation dependencies
# live in requirements-dev.txt and requirements-docs.txt.

-r requirements-minimal.txt

# Constraint folding (--constraint) and RNApuzzler template-free layouts.
# Imported lazily, see utils/shared.py and utils/rnapuzzler.py.
viennarna==2.7.2

# RNAView and FR3D base-pair extraction (r2dt.py pdb).
# Imported lazily, see utils/rnaview.py and utils/fr3d.py.
biopython==1.84
